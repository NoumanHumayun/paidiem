/**
 * A link to a certain page, an anchor tag
 */

import styled from "styled-components";

const A = styled.a`
  font-size: 28px;
  font-weight: bold;
  &:hover {
    color: #41addd;
    cursor: pointer;
  }
`;

export default A;
